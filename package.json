{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0",
    "typescript": "^4.4.2"
  },
  "scripts": {
    "frontend": "lerna run start --parallel --scope ./packages/frontend --scope ./packages/shared"
  },
  "workspaces": {
    "packages": [
      "packages/frontend/*",
      "packages/backend/*",
      "packages/shared/*"
    ]
  }
}
